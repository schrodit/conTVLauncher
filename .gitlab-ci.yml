image: node:8.3.0

before_script:

stages:
  - build
  - deploy

build_all:
  stage: build
  script:
    - npm install
    - npm run build


deploy_staging:
  stage: deploy
  script:
    - echo "Deploy to staging server"
  environment:
    name: Staging
  only:
  - master